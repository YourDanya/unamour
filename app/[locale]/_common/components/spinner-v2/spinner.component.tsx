import {SpinnerProps} from 'app/[locale]/_common/components/spinner-v2/spinner.types'

const Spinner = (props: SpinnerProps) => {

    return (
        <div className={'spinner-v2 spinner'}>
        </div>
    )
}

export default Spinner